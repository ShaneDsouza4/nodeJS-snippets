- Mongo is No-SQL Document Based Database.
- Strong support for Aggregation Pipelines.
- Works on BSON format. like JSON.
- Best for Node Applications.
- SQL works on relations, Tables. MongoDB works on 

MongoDB has Collections. 
SQL has tables.

Collections contain multiple Documents.

- Run installation.

> Connection with NodeJS
- Previously we were performing CRUD using FS and a json file.

- First check if MongoDB demon service is running;
> mongosh

- To make connection
> npm i mongoose

In mongoose there is a Schema, which is the data structure.
Schema - Define the structure
	Schema -> Model
	Using Model we do CRUD operations.

_id is generated by MongoDB.

-In schema can pass timestamps true